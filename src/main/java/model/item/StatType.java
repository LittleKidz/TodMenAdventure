package model.item;

/**
 * Stat types that TomeItem randomly boosts after defeating a Goblin.
 */
public enum StatType {
    /** Maximum HP. */
    MAX_HP,
    /** Attack power. */
    ATTACK,
    /** Defense. */
    DEFENSE
}
